import { Router } from "express";

const router = Router();

// Verificar si un usuario existe
router.get('/check/:identificacion', );

// Registrar un nuevo usuario
router.post('/register', );




export default router;
